## Phase 0 - repository and specification reconciliation

- [x] 0.1 Before runtime edits, confirm `proposal.md`, `design.md`, `tasks.md`, and capability spec agree on `AUTH_MODE=local|google`, backend provider enforcement with `404 AUTH_PROVIDER_DISABLED`, nullable federated password hashes, explicit `create_local_user`/`create_federated_user` paths, exact Google identity and authoritative-email claims, Google `sub` identity, no email auto-link, shared Dental login finalization, explicit Drive consent, Google-mode same-account Drive binding through `login_hint` plus `about.get`, local-mode manual Drive account choice, POST-only OAuth handoff with backend-built URL and `window.location.assign`, callback `303` result transport versus direct sanitized `400` security failure, `409` only for same-origin JSON, denial without logout, redirect-based auto-onboarding, GIS popup callback/CSRF/COOP/FedCM contract, migration-head discovery, and GIS/Picker CSP discovery. Run `openspec validate google-drive-managed-workspace --strict` and stop affected work if executable repository state contradicts a closed decision.
- [x] 0.2 Inspect actual Alembic head, auth route/dependency/session behavior, user persistence, login UI/bootstrap ownership, security-header ownership, deployment environment forwarding, active-patient ownership, external HTTP mocking, and frontend test seams. Record exact affected modules and migration graph before writing a migration.
- [x] 0.3 Inspect `ClinicalAssistant`, `ClinicalAssistantArea`, `ClinicalTranscript`, `ClinicalComposer`, `MarkdownRenderer`, `AppShell`, `Sidebar`, `VideoExplorer`, Tailwind tokens, responsive tests, and clinical Playwright prior art. Confirm generic utility/accessory seam, safe Markdown path, raw official `PickerBuilder`, and approved incremental shadcn workflow. Before shadcn changes, inspect project context and generated diff; accept only `Resizable`, `Sheet`, `ScrollArea`, `AlertDialog`, and `Alert` plus required dependencies.

## Phase 1 - Google identity and authentication mode

- [x] 1.1 Add fail-first backend coverage for default/local mode, Google mode, invalid mode startup failure, incomplete Google-mode configuration, safe `GET /api/auth/config`, `AUTH_MODE=local` disabling `POST /api/auth/google` with `404 AUTH_PROVIDER_DISABLED`, `AUTH_MODE=google` disabling `POST /api/auth/signup` and `POST /api/auth/login` with the same response and no session creation, exact Google `aud`/issuer/`exp`/`sub`/email/`email_verified` claims, Gmail/Workspace authority policy, `403 GOOGLE_EMAIL_NOT_AUTHORITATIVE`, invalid request Origin/Fetch Metadata/content type before token verification, provider failure sanitization, credential/raw-payload log prohibition, explicit federated creation with null `password_hash`, null-password local login generic `401`, existing identity reuse, new identity creation, concurrent identity creation, unique provider subject and user/provider, existing-email collision returning `409 GOOGLE_ACCOUNT_LINK_REQUIRED`, shared `last_login_at`/Circle/member/session finalization, Dental session-cookie convergence, `/api/auth/me`, logout, and unchanged local signup/login behavior.
- [x] 1.2 Add fail-first frontend coverage for backend-owned mode selection, existing local form in local mode, official GIS-rendered button with JavaScript popup callback in Google mode, no local controls/One Tap/auto-select in Google mode, immediate `Iniciando sesión…`, `Cuenta verificada`, session hydration, same-origin JSON callback with credentials included, sanitized failures, and one discriminated bootstrap state without unrelated loading/auth/Drive booleans.
- [x] 1.3 Inspect current Alembic head, create the next identity migration from that head, relax `users.password_hash` to nullable, add explicit local/federated repository paths, create `auth_identities` keyed by unique `(provider, provider_subject)` and `(user_id, provider)`, and verify graph. Google `sub` is authority and email is snapshot only. Never infer migration ordinal from this OpenSpec.
  - Notes: head was `0011_add_clinical_turn_outcomes`; added `0012_add_auth_identities` (down_revision `0011`), verified single head via `alembic heads`/`history`, applied to dev Postgres (`alembic_version` = 0012; `password_hash` nullable; both unique constraints + FK CASCADE present). `users_repo` gains `create_local_user`, `create_federated_user` (NULL hash), `find_identity`, `create_identity` (both UniqueViolationError paths proven via savepoint check on live DB); `create_user` untouched for existing signup callers. ruff check/format + mypy clean; existing `test_auth.py`/`test_auth_circle.py` pass (27).
- [x] 1.4 Implement validated backend `AUTH_MODE`, backend provider enforcement with `404 AUTH_PROVIDER_DISABLED`, safe `/api/auth/config`, GIS request-context guard before token verification, Google identity verification through Google's maintained verifier, exact identity and authoritative-email acceptance, stable sanitized domain errors, provider identity lookup/create/collision policy, explicit federated-user persistence, shared login finalization, and issuance of the existing Dental JWT/session cookie. Keep `get_current_user()` as downstream seam; do not add parallel Google application-session contexts.
  - Notes: `config.py` validates `AUTH_MODE` (RuntimeError on unknown/incomplete google mode), adds `GOOGLE_CLIENT_ID`, `APP_ORIGINS`, `GOOGLE_DRIVE_ENABLED`, `GOOGLE_DRIVE_AUTO_ONBOARD`. `routes/auth.py` gains `GET /auth/config`, `POST /auth/google` (mode gate → Origin/Fetch-Metadata/content-type guard → `verify_google_id_token` via `google.oauth2.id_token` in `asyncio.to_thread` → exact sub/email/email_verified/hd claims → Gmail/hd authority policy → `find_identity` reuse with snapshot refresh, else 409 collision or transactional federated-user+identity create with concurrent-winner adoption → `_finalize_dental_login`), shared `_finalize_dental_login` refactor, signup/login gated to local mode, login null-hash generic 401. `users_repo.update_identity_email_snapshot` added. `google-auth>=2.29` dependency added (uv lock updated). Fail-first `test_auth_mode_google.py` 19/19 green; full backend suite 444 passed / 67 skipped; ruff + mypy clean.
- [x] 1.5 Implement mode-driven login/bootstrap UI and typed API calls through `src/lib/api.ts`. Local mode keeps current flow. Google mode uses official GIS popup callback with `ux_mode=popup`, `auto_select=false`, `use_fedcm_for_button=false`, no One Tap `prompt()`, and no `login_uri`/redirect flow; never imitates provider branding manually. Use Spinner for indeterminate provider work and Skeleton only for known content layout.
  - Notes: typed calls live in existing `src/lib/authApi.ts` (the repo's auth fetch seam, already the module the 1.2 fail-first tests mock): `getAuthConfig()` -> `GET /api/auth/config`, `loginWithGoogle(credential)` -> `POST /api/auth/google` (`{"credential"}`, `credentials: include`, JSON). `useAuth` now exposes one discriminated `status` union (`loading-config | unauthenticated-local | unauthenticated-google | authenticating-google | establishing-session | checking-drive | authorizing-drive | preparing-workspace | ready | ready-without-drive | error`) plus `authConfig` and `loginWithGoogle`; helpers `isAuthenticatedStatus`/`isUnauthenticatedStatus` mapped into `App.tsx` RequireAuth and `AdminVideos` guards (legacy `loading/anon/authed` removed). `Login` renders local form only in `unauthenticated-local`, official GIS container (testid `google-signin-button`) in `unauthenticated-google` with GIS script lazy-loaded, `ux_mode=popup`, `auto_select=false`, `use_fedcm_for_button=false`, `button_auto_select=false`, no `login_uri`/`prompt()`; immediate `Iniciando sesión…` -> `Cuenta verificada` via Spinner (no percentages); sanitized failures never echo the credential; navigates to `returnTo` on `ready`. Drive-bootstrap states (`checking-drive`, `authorizing-drive`, `preparing-workspace`) are declared in the union but intentionally unwired — they belong to task 3.2. Fail-first `authBootstrapMode.test.tsx` 7/7 green; frontend suite 285/285 pass; `tsc --noEmit`, biome, and production build clean. (Local machine has no `bun`; ran `node node_modules/typescript/bin/tsc`, `node node_modules/vitest/vitest.mjs run`, `node_modules/@biomejs/cli-win32-x64/biome.exe`, `node node_modules/vite/bin/vite.js`.)
- [x] 1.6 Run focused auth migration, repository, route, configuration, frontend, and log-safety tests. Prove nullable federated users cannot use local password login, authoritative-email rejection occurs before side-effects, local and Google successful sign-ins share `last_login_at`/Circle/member/session finalization, and requests become indistinguishable after `session cookie -> get_current_user() -> Dental user_id`.
  - Notes: evidence collected: migration `0012_add_auth_identities` verified in 1.3; `test_auth_mode_google.py` (19) proves mode enforcement (`404 AUTH_PROVIDER_DISABLED` both directions, no session creation), safe `/api/auth/config`, GIS request-context guard before verification, invalid/unverified/non-authoritative claim rejection (`401`/`403` before identity/session side-effects), email-collision `409`, identity reuse/creation, null-password local login -> generic `401` (`test_null_password_user_gets_generic_401`), shared finalization (`last_login_at` "now" on both local and Google paths), session-cookie convergence (`/api/auth/me` + logout in Google mode), and credential log prohibition (`test_google_credentials_never_reach_logs`; route emits no credential logging). Frontend `authBootstrapMode.test.tsx` (7) + full Vitest suite (285) green. Full backend suite 477 passed / 67 skipped; ruff check/format + mypy (129 files) clean; frontend tsc + biome + production build clean.

## Phase 2 - Drive connection and security boundary

- [x] 2.1 Add fail-first cipher, repository, configuration, and authenticated Drive OAuth tests for complete/partial configuration, exact `drive.file`, one-shot state bound to initiating Dental user/session, Google-mode expected-email transaction binding and backend `login_hint`, `about.get(fields=user(permissionId,emailAddress))` account confirmation, same-account success/mismatch, mismatch revoke/no-persistence/no-workspace/prior-connection-preservation, local-mode manual account changes, omitted-scope-as-requested semantics, unexpected scope rejection, refresh-token requirement, purpose-separated AES-GCM keyring/lazy rotation, request-local access tokens, active/disconnected/revoked persistence, same-origin mutations, no code-exchange/revoke retry, reconnect account changes, callback sanitization, disconnect local clear, and log prohibition.
  - Notes: 4 fail-first files added, 72 tests collected, all red (expected until 2.2). `test_google_drive_config.py` (10): complete/partial/malformed keyring validation, absent-config starts with `GOOGLE_DRIVE_CONFIGURED=False`, keyring independent of `JWT_SECRET`. `test_drive_token_cipher.py` (11): AES-GCM roundtrip, fresh 12-byte nonce, plaintext absence, wrong user/purpose/tamper/unknown-version fail closed, lazy rotation re-encrypts under active key, `token_cipher` never imports `JWT_SECRET`. `test_google_drive_oauth_integration.py` (13, respx): exact authorization-URL params, omitted-scope semantics, `GOOGLE_DRIVE_SCOPE_MISSING`/`SCOPE_UNEXPECTED` on missing/extra scope or missing refresh token, exchange/revoke exactly-one-call (no retry), `about.get` with exact `fields=user(permissionId,emailAddress)`, folder create with exact markers. `test_google_drive_oauth_routes.py` (38): status/config mapping, POST-only start (405 on GET), same-origin 403 before mutation, `authorization_url`-only response, HttpOnly/Secure/lax 600s state cookie, Google-mode server-side `login_hint` + 409 without identity, callback 400 `GOOGLE_DRIVE_OAUTH_STATE_INVALID` (missing/mismatch/expired/wrong-user/session-fingerprint/replay) before exchange, 303 allowlist results, mismatch best-effort revoke + prior-connection preservation + no workspace, local-mode account change clears folder/binding identity, reconnect same-account preserves folder, disconnect clears locally even when remote revoke fails, request-local tokens never persisted/logged; repository persistence invariants deferred to 2.4 live-DB proof (in-memory repo double pins route orchestration). ruff check/format + mypy clean on the 4 files; rest of suite 477 passed / 67 skipped unchanged.
- [x] 2.2 From identity migration head, create next Drive workspace migration and verify graph. Add connection/OAuth-transaction repository including short-lived expected Google email, token cipher, async OAuth/Drive/about adapter, authenticated status/start/callback/disconnect routes, stable errors, route registration, required dependency/lock changes, and deployment configuration. `POST /api/google-drive/oauth/start` is the only start contract: it creates state and returns a backend-built `authorization_url`; it has no GET fallback or arbitrary return target. Never reuse `JWT_SECRET` for Drive encryption.
  - Notes: migration `0013_add_google_drive_workspace` (down_revision `0012`): `google_drive_connections` (one row per user; checked `status`; terminal states clear refresh credentials/scopes but retain account/folder identity + encrypted binding secret) + `google_drive_oauth_transactions` (BYTEA state_hash PK, user/session-fingerprint/expected-email/folder-op binding, used_at). `alembic heads` = single 0013, history 0012 -> 0013 verified. `config.py` validates complete-or-absent Drive config (RuntimeError on partial/malformed keyring/unknown active version; keyring independent of JWT_SECRET). `auth/token_cipher.py` AES-256-GCM keyring: fresh 12-byte nonce, user/purpose/version AAD, lazy re-encrypt under active key, fail-closed ValueError. `integrations/google_drive_oauth.py` (exact `drive.file` URL, single-shot exchange with scope/refresh validation raising `GoogleDriveOAuthError(code, access_token)`, never-retried best-effort revoke, request-local refresh) + `integrations/google_drive.py` (`about.get` exact `fields=user(permissionId,emailAddress)`, folder create with exact markers) via httpx.AsyncClient. `db/google_drive_repo.py` owns all SQL (get/create/replace connection, set_disconnected/set_revoked, create/get/claim transaction — claim is atomic one-shot UPDATE ... WHERE used_at IS NULL). `users_repo.get_google_identity` added. `routes/google_drive.py`: status, POST-only start (explicit GET -> 405 so the SPA catch-all cannot swallow it), callback (cookie-anchored state; state/fingerprint/expiry validated BEFORE strict JWT decode so wrong-session returns 400 not 401; claim before exchange; 303 with allowlisted result on handled outcomes; direct sanitized 400 GOOGLE_DRIVE_OAUTH_STATE_INVALID before exchange otherwise; never 409), disconnect (best-effort revoke, always clears locally, idempotent). Router mounted in `main.py`; `/api/google-drive/*` added to the validation-error sanitizer. `cryptography` declared as direct dependency (uv lock updated; already transitive before). `deploy/.env.example` documents the six-value all-or-nothing Drive block. Fail-first suites green: cipher 11, config 10, oauth integration 14, routes 37 (one contract-test fake fixed: `raise SimpleNamespace(...)` would raise TypeError and drop the attached `code`/`access_token`; replaced with a real exception carrying the same attributes — same seam contract, test intent unchanged). Full backend suite 549 passed / 67 skipped; ruff check/format + mypy clean.
- [x] 2.3 Preserve exact Drive boundary: encrypted refresh token, encrypted per-connection binding secret, access token request-local except no-store Picker response, one authoritative folder, exact `drive.file`, post-exchange `about.get` account confirmation in Google mode, fixed safe callback/return targets, and Dental session survival across denial, account mismatch, disconnect, revocation, and reconnect failure.
  - Notes: boundary enforced in `routes/google_drive.py` + `auth/token_cipher.py`. Refresh token + binding secret persist only as AES-GCM ciphertext under the versioned keyring (route tests prove `"access-token-xyz" not in str(row)`); access tokens are function-local (never passed to repo or logged; httpx INFO request lines suppressed app-wide so callback URLs carrying the one-shot code cannot reach logs). Google-mode callback: expected email from server-side `auth_identities` snapshot (`login_hint` backend-generated), post-exchange `about.get` normalized-email equality required before any persistence/workspace; mismatch best-effort-revokes returned credentials, preserves prior connection, no folder creation, `303 result=account_mismatch` — never HTTP 409. Local mode permits explicit account change (new binding secret, old folder identity cleared); same-account reconnect reuses folder + binding identity. Consent denial/provider failure -> `303 consent_denied|provider_error` with no persistence and no logout. Denial/mismatch/disconnect/revoke/reconnect-failure paths never touch the Dental session. Fixed return target only: `303` to `config.GOOGLE_DRIVE_RETURN_URL` with exactly the allowlisted `result` param. Evidence: `test_google_drive_oauth_routes.py` (37) covers success, missing refresh, exchange failure, scope error revoke, consent denied, provider error, both mismatch variants, local account change, reconnect, disconnect idempotency/remote-revoke-failure, replay/wrong-user/wrong-session/missing/mismatch/expired state, and credential log prohibition; cipher suite (11) proves ciphertext-only persistence, purpose separation, wrong user/tamper/unknown-version fail-closed, lazy rotation, no JWT_SECRET import.
- [x] 2.4 Run focused connection/cipher/config/OAuth tests and inspect migrated schema. Prove cross-session/cross-origin rejection, exact scope, ciphertext-only persistence, Google-mode same-account email equality, mismatch cleanup with prior-connection preservation, local-mode account-change binding replacement, durable revoked state, disconnect cleanup, callback sanitization, and zero credential leakage.
  - Notes: focused suites (cipher 11 + config 10 + integration 13 + routes 38 = 72) green. Live-DB proof added: `tests/test_google_drive_repo_live.py` (8 tests) runs the real `google_drive_repo` SQL against a scratch Postgres (`drive_live_test` on the dev container, migrated to single head 0013 via `alembic upgrade head`; DSN passed through `DRIVE_LIVE_TEST_DSN`, module skips otherwise). Proves: schema contract (columns, `status` CHECK constraint, FK CASCADE, `state_hash` PK), ciphertext-only persistence (plaintext refresh/binding bytes absent from the raw row; decrypt roundtrips), one-row-per-user (second insert raises `UniqueViolationError`; `replace_active_connection` overwrites), durable revoked state (refresh credentials/scopes/pending-op cleared; account/folder/binding identity retained across refetch), disconnect cleanup with identity retention, atomic one-shot transaction claim (second claim None; only the 32-byte hash persisted, raw state absent), status CHECK violation rejection, and user-delete cascade. Zero credential leakage: `test_drive_credentials_never_reach_logs` (access/refresh token, one-shot code, client secret never in logs). Full backend suite 558 passed / 67 skipped; ruff check/format + mypy (140 files) clean.
- [x] 2.5 Make OAuth callback transport explicit and testable: after valid one-shot state, success and handled consent/domain/provider failures return `303 See Other` to the fixed Assistant URL with only allowlisted `result=connected|consent_denied|account_mismatch|provider_error`; invalid or replayed state, expiry, wrong Dental user, or wrong session returns sanitized HTTP `400` with code `GOOGLE_DRIVE_OAUTH_STATE_INVALID` directly before code exchange; browser callback never emits HTTP `409`, which remains reserved for same-origin JSON endpoints.
  - Notes: transport contract in `routes/google_drive.py` `oauth_callback` (cookie-anchored state, claim before exchange, 303 allowlist, direct 400 for invalid security state). New `test_callback_transport_matrix_never_returns_409` runs the full matrix: connected/account_mismatch/consent_denied/provider_error → 303 with exactly the allowlisted `result` (never 409), invalid state → sanitized 400 `GOOGLE_DRIVE_OAUTH_STATE_INVALID` with no exchange. Existing tests cover missing/mismatched/expired/replayed/wrong-user/wrong-session 400-before-exchange and POST-only start with 409 reserved for same-origin JSON (Google-mode missing identity). Evidence: `test_google_drive_oauth_routes.py` 38/38; full suite 558 passed / 67 skipped.

## Phase 3 - seamless authentication to Drive bootstrap

- [ ] 3.1 Add fail-first frontend and browser coverage for `loading-config`, unauthenticated local/Google, Google authentication, Dental session establishment, Drive status check, authorization handoff, same-account confirmation/mismatch, workspace preparation, `ready`, `ready-without-drive`, and sanitized error variants.
- [ ] 3.2 Implement post-login sequence only after authoritative `/api/auth/me`: request `/api/google-drive/status`; enter Assistant immediately for healthy connection; when disconnected and auto-onboard is enabled, call `POST /api/google-drive/oauth/start` with Dental session credentials included, receive the backend-built `authorization_url`, and execute `window.location.assign(authorization_url)`; in Google mode use server-derived verified email as `login_hint`, verify callback Drive `about.user.emailAddress`, map mismatch to domain `GOOGLE_DRIVE_ACCOUNT_MISMATCH` with browser callback `303 See Other` and `result=account_mismatch`, never HTTP `409`, without persistence/workspace or prior-connection replacement; in local mode allow explicit account choice; invalid callback state returns sanitized `400` before exchange; never use GET, a form fallback, a browser-built provider URL, a browser-supplied `return_to`, or a chained popup; use the fixed return target; when Drive is denied/unavailable, preserve Dental session and enter `ready-without-drive` with `Google Drive no está conectado`, explanatory copy, and `Conectar Drive` action.
- [ ] 3.3 Prove returning Google user receives no Drive prompt/account chooser, first-time user receives explicit provider consent and same-account verification, mismatch revokes returned credentials, redirects `303` with `result=account_mismatch`, and never emits callback HTTP `409`, denial returns `303 result=consent_denied` without logout, invalid state returns sanitized `400` before exchange, Drive reconnect failure does not invalidate session, local mode does not require Google credentials or auto-onboarding and permits manual account choice, and the frontend POST-to-`authorization_url` handoff calls `window.location.assign` exactly once.

## Phase 4 - managed patient Drive API

- [ ] 4.1 Add fail-first mocked Google and HTTP coverage for authoritative folder creation/reuse/missing/recovery, exact operation markers, possible-orphan acknowledgement, signed patient/file binding and tamper rejection, no blind uncertain-write retry, `.txt`/`text/plain` enforcement, deterministic Markdown export fixtures, 1 MiB persisted/read limit, 8 MiB pre-JSON route limit, patient ownership, pre-pagination scope, bounded opaque IDs/cursors, version conflict, managed-source rejection, immutable import source, no-store Picker token, revoked response, validation sanitization, and metadata-only logs.
- [ ] 4.2 Implement bounded async Drive REST adapter and route orchestration for folder reconciliation/recreation, HMAC patient/file binding, patient-scoped list/search/read/create/update, deterministic TXT export, stable operation IDs, one reconciliation, optimistic version protection, patient-verified Picker token, and Markdown/TXT import-copy. Keep all SQL in repository and all frontend fetches in `src/lib/api.ts`.
- [ ] 4.3 Run focused managed-folder/file tests. Prove only metadata-valid patient-bound managed TXT is returned, ambiguous writes resolve to success or unknown without retry, source files remain untouched, content/transport limits hold, and no token/title/content/search term enters logs.

## Phase 5 - workspace UI

- [ ] 5.1 Add fail-first AppShell, Sidebar, Drive state/component, API, accessibility, and serializer tests covering unchanged Biblioteca behavior, desktop resizable sidecar, mobile accessible Sheet, approved primitive allowlist, exact connection/recovery copy, one dominant action, immediate feedback, patient-scoped paging, Preview-first existing files, Edit-first drafts, shared buffer, byte-faithful TXT identity, safe Markdown preview, visible `.md` to `.txt` normalization, manual save, conflict/unknown preservation, and no network effects during Preview/Edit switching.
- [ ] 5.2 Add reviewed shadcn primitives only after context inspection/dry run/diff acceptance. Implement generic shell utility/accessory APIs, patient-aware discriminated Drive state, compact list/search/import UI, application-native TXT Preview, local Markdown Preview/Edit, shared authoring buffer/plain-text baseline, deterministic serializer, manual save state, recovery Alerts, consequence-only AlertDialogs, desktop `Resizable`, and mobile `Sheet`.
- [ ] 5.3 Preserve UX laws: one dominant action, comfortable adjacent targets, familiar labels, immediate feedback without fake percentages, grouped metadata without file cards, unequivocal completion, hidden provider/crypto internals, and user control over denial/revocation/local drafts.

## Phase 6 - clinical transfer and Picker

- [ ] 6.1 Add fail-first coverage for AppShell dirty transition guard, same-origin and programmatic navigation, bypass links, one queued continuation, browser unload, full/selection insertion without submit, Assistant message and structured-draft local preparation without write, stable operation IDs, raw PickerBuilder token lifetime, imported copy behavior, and patient match on every file/editor/Picker/transfer action.
- [ ] 6.2 Implement dirty save/discard/cancel guard, explicit Drive-to-composer insertion, explicit Assistant-to-Drive local drafts, raw official Picker integration, immutable existing names, `DRIVE_WRITE_UNKNOWN` recovery, and no autonomous Drive/LLM/RAG/SSE action.
- [ ] 6.3 Capture actual GIS and Picker browser network requirements. Finalize exact CSP origins while preserving exact Google Fonts origins and prohibiting wildcard sources, `unsafe-eval`, and unrelated third parties. Fix V1 GIS to official popup callback with One Tap and auto-select disabled, `use_fedcm_for_button=false`, and `Cross-Origin-Opener-Policy: same-origin-allow-popups`; verify credential callback, no blank/lost popup, and behavior on FedCM-capable and non-FedCM browsers. Add automated security-header tests and one browser smoke verification.

## Phase 7 - full verification and release evidence

- [ ] 7.1 Run focused frontend and mocked Playwright matrices for local login and backend-disabled Google auth, Google login and backend-disabled local signup/login, exact Google claim/authority acceptance/rejection, federated null-password behavior, shared login finalization, GIS popup callback/CSRF/COOP/FedCM behavior, Google first login plus explicit same-account Drive consent, same-account mismatch cleanup, returning Google login without Drive prompt, POST OAuth-start handoff via `window.location.assign` without GET/provider URL construction/popup, Google login plus Drive denial, later Drive revocation, local/Google downstream convergence, all Drive workspace states, responsive behavior, dirty transitions, conflict/unknown recovery, Picker imports, and TXT fidelity.
- [ ] 7.2 Run complete backend Ruff check/format, mypy, pytest; frontend TypeScript, Biome, Vitest, build, and Playwright. Verify no Dental session, clinical SSE, patient isolation, Chat, RAG, citation, quota, VideoExplorer, or proxy-trust regression.
- [ ] 7.3 Collect release evidence for separate test/production Google Cloud projects, verified domain, homepage/privacy/Limited Use disclosures, branding/support contact, exact origins/callback/return URLs, official GIS popup/COOP/FedCM browser behavior, test users, key restrictions, applicable Google verification, encryption-key lifecycle, manual synthetic auth/OAuth/Picker smoke, same-account/mismatch handling, denial, revoke/reconnect, and workspace recovery. Keep production Google mode/Drive disabled until gates pass.
- [ ] 7.4 Run requirement-to-proof audit and `openspec validate google-drive-managed-workspace --strict`. Prepare sync/archive only after all evidence passes.
- [ ] 7.5 Run explicit callback transport matrix: valid success and handled consent/domain/provider failures produce `303 See Other` plus allowlisted `result`; missing/mismatched/expired/replayed state and wrong Dental session produce direct sanitized `400` before exchange; callback never emits `409`, while same-origin JSON conflicts retain `409` semantics.

## Execution graph

```text
Phase 0 OpenSpec and repository reconciliation
  -> Phase 1 Google identity and AUTH_MODE
  -> Phase 2 Drive connection/security boundary
  -> Phase 3 authentication-to-Drive bootstrap
  -> Phase 4 managed patient Drive API
  -> Phase 5 workspace UI
  -> Phase 6 clinical transfer and Picker
  -> Phase 7 full verification and release evidence
```

No Drive managed-file work may precede identity/session and Drive connection foundations. If executable repository state invalidates a detail, stop that slice, document exact conflict, reconcile OpenSpec first, then continue under updated contract.
