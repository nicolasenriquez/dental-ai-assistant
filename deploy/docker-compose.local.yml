services:
  app-blue:
    ports:
      - "8000:8000"
    environment:
      SEED_ENABLE: "true"
      VOICE_TRANSCRIPTION_ENABLED: ${VOICE_TRANSCRIPTION_ENABLED:-false}
      VOICE_LANGUAGE: ${VOICE_LANGUAGE:-es}
